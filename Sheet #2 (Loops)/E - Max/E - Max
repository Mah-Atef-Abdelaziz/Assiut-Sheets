#include <iostream>

using std::cin;
using std::cout;
using std::max;

int main()
{
    int N, num;
    cin >> N;
    cin >> num;
    int Maximum = num;
    for (int i = 0; i < N - 1; i++)
    {
        cin >> num;
        Maximum = max(Maximum, num);
    }
    cout << Maximum;
    return 0;
}
