#include <iostream>

using std::cin;
using std::cout;

int main()
{
    int N;
    cin >> N;
    for (int i = 0; i < 12; i++)
    {
        cout << N << " * " << i + 1 << " = " << N * (i + 1) << "\n";
    }

    return 0;
}
