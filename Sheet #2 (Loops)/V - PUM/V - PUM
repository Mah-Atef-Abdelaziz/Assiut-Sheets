#include <iostream>

using std::cin;
using std::cout;

int main()
{
    int N;
    cin >> N;
    for (int i = 0; i < N; i++)
    {
        for (int j : {1, 2, 3})
        {
            cout << j + 4 * i << " ";
        }
        cout << "PUM\n";
    }

    return 0;
}
