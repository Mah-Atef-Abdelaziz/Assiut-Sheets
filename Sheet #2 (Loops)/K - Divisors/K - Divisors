#include <iostream>

using std::cin;
using std::cout;

int main()
{
    int N;
    cin >> N;
    for (int i = 1; i <= N; i++)
    {
        if (N % i == 0)
        {
            cout << i << "\n";
        }
    }
    return 0;
}
