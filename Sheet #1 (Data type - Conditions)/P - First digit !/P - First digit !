#include <iostream>
#include <cmath>

using std::cin;
using std::cout;
using std::log10;
int main()
{
    int X, FirstDigit;
    cin >> X;
    int length = log10(X) + 1;
    FirstDigit = X / pow(10, length - 1);
    if (FirstDigit % 2 == 0)
    {
        cout << "EVEN";
    }
    else
    {
        cout << "ODD";
    }

    return 0;
}
