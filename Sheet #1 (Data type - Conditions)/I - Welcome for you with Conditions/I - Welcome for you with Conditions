#include <iostream>
#include <cmath>

using std::cin;
using std::cout;

int main()
{
    int A, B;
    cin >> A >> B;
    if (A >= B)
    {
        cout << "Yes";
    }
    else
    {
        cout << "No";
    }

    return 0;
}
