#include <iostream>
#include <string>

using std::cin;
using std::cout;

int main()
{
    long long A, B, C, D;
    cin >> A >> B >> C >> D;
    cout << "Difference = " << (A * B) - (C * D);
    return 0;
}
