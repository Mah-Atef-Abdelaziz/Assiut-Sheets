#include <iostream>
#include <cmath>

using std::cin;
using std::cout;

int main()
{
    long long N;
    cin >> N;
    long long sum = 0;
    sum = (N * (N + 1)) / 2;
    cout << sum;
    return 0;
}
