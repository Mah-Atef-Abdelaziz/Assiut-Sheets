#include <iostream>
#include <cmath>

using std::cin;
using std::cout;

int main()
{
    long long N, M;
    cin >> N >> M;
    int NLast = N % 10;
    int MLast = M % 10;
    cout << NLast + MLast;
    return 0;
}
