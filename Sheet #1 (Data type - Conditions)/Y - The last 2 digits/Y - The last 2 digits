#include <iostream>

using std::cin;
using std::cout;

int main()
{
    int A, B, C, D;
    cin >> A >> B >> C >> D;
    int a = A % 100;
    int b = B % 100;
    int c = C % 100;
    int d = D % 100;
    int result = (a * b * c * d) % 100;
    if (result <= 9)
    {
        cout << "0" << result;
    }
    else
    {
        cout << result;
    }

    return 0;
}
