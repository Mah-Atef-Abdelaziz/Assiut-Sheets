#include <iostream>
#include <string>

using std::cin;
using std::cout;
using std::string;

int main()
{
    string x, y;
    cin >> x >> y;
    int result = x.compare(y);
    result <= 0 ? cout << x
                : cout << y;
    return 0;
}
