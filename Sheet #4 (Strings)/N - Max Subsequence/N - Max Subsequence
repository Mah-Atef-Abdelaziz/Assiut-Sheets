#include <iostream>
#include <string>

using std::cin;
using std::cout;
using std::string;

int main()
{
    int size;
    cin >> size;
    string S;
    cin >> S;
    int c = 0;
    for (int i = 0; i < size; i++)
        if (S[i] != S[i + 1])
            c++;
    cout << c;
    return 0;
}
