#include <iostream>

using std::cin;
using std::cout;
using std::string;

int main()
{
    string s;
    getline(cin, s);
    for (char c : s)
    {
        if (c == '\\')
            break;
        cout << c;
    }
    return 0;
}
