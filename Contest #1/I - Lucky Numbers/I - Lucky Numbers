#include <iostream>

using std::cin;
using std::cout;

int main()
{
    int N;
    cin >> N;
    int d1 = N / 10;
    int d2 = N % 10;

    if (d2 != 0 && d1 % d2 == 0)
    {
        cout << "YES";
    }
    else if (d1 != 0 && d2 % d1 == 0)
    {
        cout << "YES";
    }
    else
    {
        cout << "NO";
    }

    return 0;
}
