#include <iostream>
#include <cmath>

using namespace std;

#define ll long long

int main()
{
    ll A, B, Q;
    cin >> A >> B >> Q;
    ll res = Q % 3;
    if (res == 1)
        cout << A;
    else if (res == 2)
        cout << B;
    else
        cout << (A ^ B);
    return 0;
}
